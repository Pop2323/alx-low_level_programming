#include "main.h"

/**
 * _islower -  function that checks for lowercase character
 *
 * @c: check the number ascii
 *
 * Return: return 1 if it is lowercase else reurn 0
*/

int _islower(int c)
{
	if (C >= 97 && c <= 122)
		return (1);
	return (0);
}
